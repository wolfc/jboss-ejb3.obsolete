<project name="jboss-ejb3-endpoint" default="install">
   <import file="setup.xml"/>

   <target name="install">
      <copy file="lib/jboss-ejb3-endpoint.jar" todir="${jboss.home}/common/lib" verbose="${verbose}"/>
   </target>

   <target name="uninstall">
      <delete file="${jboss.home}/common/lib/jboss-ejb3-endpoint.jar" verbose="${verbose}"/>
   </target>
</project>
